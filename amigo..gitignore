# Ignorar entornos y compilaciones pesadas (NUNCA subir esto)
env/
__pycache__/
build/
dist/
SocialBotFarm.spec

# (Fíjate que ya NO estamos ignorando cuentas.db, ni profiles/, ni logs/, ni los .json)